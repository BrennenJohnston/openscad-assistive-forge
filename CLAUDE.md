# Claude Code project rules

Read `AGENTS.md` at the repository root — it carries the golden rules
every AI agent working in this repository follows: protected files, the
accessibility bar, commit conventions, and the quality gates that must
run before any PR. The same rules in Cursor's format live under
`.cursor/rules/`.
